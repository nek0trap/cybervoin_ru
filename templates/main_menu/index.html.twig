{% extends 'base.html.twig' %}


{% block body %}
    <p style="padding: 100px 300px"> Now, repository is in work , we try fast complete :) </p>
    <p style="padding: 0px 300px; float: right"> <span class="badge badge-info">Info</span> Your roles in system: <br>
        {% for role in app.user.roles %}
            <br>
            {{ role }} <br>
            {% if role == 'ROLE_ADMIN' %}
                <a href="/admin">Admin Panel</a> <br>
                <a href="/admin/help/createUser">Create User</a><br>
                <a href="{{ path("admin_char_list") }}">Characters List</a>
            {%endif %}
            {% if role == 'ROLE_GAMEMASTER' %}
                <a href="{{ path("gamemaster_create_char") }}">Create Character for game</a>
                <a href="{{ path("gamemaster_list_char") }}">Character List</a>
            {% endif %}
    {% endfor %}</p>

{% endblock %}
