{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">

            </div>

            <div class="col">
                {{ form(form, {'action' : path(back_path), 'method': 'POST'}) }}
            </div>

            <div class="col">

            </div>
        </div>
    </div>
{% endblock %}